### Compilazione su Linux (o Unix based)
Per compilare il file eseguire il seguente comando nel terminale della cartella:
```gcc tris.c -o tris.out```


Per eseguire il programma digitare:
```./tris.out```
